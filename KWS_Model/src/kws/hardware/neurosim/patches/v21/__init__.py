"""DNN+NeuroSim V2.1 patch metadata.

The actual upstream-specific patch is intentionally not applied until the
source validator identifies a compatible checkout.  The build system always
works on a copied source tree.
"""
