# M1 sanity-check config: no augmentation, just confirm data+model+training loop are wired correctly.
augment: false
epochs: 15
batch_size: 128
lr: 0.001
weight_decay: 0.0001
label_smoothing: 0.1
warmup_fraction: 0.05
seed: 0
