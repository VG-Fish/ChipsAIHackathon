# Quantization-aware training characterization spike (M5) -- reporting artifact only,
# not the checkpoint handed to Phase 3 (which must stay FP32).
augment: true
epochs: 10
batch_size: 128
num_workers: 4
persistent_workers: true
prefetch_factor: 2
lr: 0.0001
weight_decay: 0.0001
label_smoothing: 0.1
warmup_fraction: 0.0
seed: 0
